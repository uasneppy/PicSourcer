[project]
name = "picsourcer"
version = "2.0.0"
description = "Telegram bot that automatically sources channel images via the Fluffle reverse-image search API"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.11.12",
    "python-dotenv>=1.0.1",
    "python-telegram-bot>=21.10",
    "pillow>=11.1.0",
]
